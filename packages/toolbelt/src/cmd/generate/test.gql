type NestedTest {
  key: [String]
}

"""
Test schema description.
"""
type Post {
  """
  Unique identifier.
  """
  id: ID!
  title: String! @blossomImpl(type: function)
  content: String @blossomImpl(type: async)
  testFunction(number: Int): Int @blossomImpl(type: function)
  testArray: [String!]! @blossomImpl(type: async)
  nestedTest: NestedTest! @blossomImpl(type: async)
}

input PostPayload {
  title: String!
  content: String!
}

type Query {
  """
  Test function description.
  """
  testQuery(id: ID!): Post
}

type Mutation {
  testMutation(
    """
    Test comments.
    """
    input: PostInput!
  ): Post
}

schema {
  query: Query
  mutation: Mutation
}
