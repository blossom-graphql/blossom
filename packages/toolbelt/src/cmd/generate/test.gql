type NestedTest {
  key: [String!]
}

"""
Test schema description.
"""
type Post {
  """
  Unique identifier.
  """
  id: ID!
  title: String!
  """
  If I put a comment here, it's gonna appear.
  """
  content: String
  testFunction(
    """
    Comments for this type.
    """
    testNumber: Int!
    testArray: [String!]!  
  ): Int
  testArray: [String!]!
  nestedTest: [NestedTest!]!
}

input PostPayload {
  title: String!
  content: String!
}

type Query {
  """
  Test function description.
  """
  testQuery(id: ID!): Post
}

type Mutation {
  testMutation(
    """
    Test comments.
    """
    input: PostInput!
  ): Post
}

schema {
  query: Query
  mutation: Mutation
}
